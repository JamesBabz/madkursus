package dk.jamesbabz.madkursus.service.models;

/** Deliberately small, typed rule catalogue; process JSON stores stable names, not executable expressions. */
public enum CookingProcessDerivedRule {
    POTATO_WATER_PER_GRAM, POTATO_SALT_PER_GRAM,
    PASTA_WATER_PER_GRAM, PASTA_SALT_PER_GRAM,
    RICE_WATER_PER_GRAM
}
